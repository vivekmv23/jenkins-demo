pipeline {
  agent any
  parameters {
    string(name:VERSION, defaultValue:"1.0.1", description:"Choose the version")
  }
  stages {
    stage("BUILD") {
      steps {
        sh 'echo "version: ${params.VERSION}"'
        sh 'echo "running BUILD stage"'
      }
    }
    stage("DEPLOY") {
      steps {
        echo "running DEPLOY"
      }
    }
    stage("TEST") {
      steps {
        echo "running TEST"
      }
    } 
  }
}
